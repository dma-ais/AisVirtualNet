TODO
====


Step 2:
-------------------------

+ Select target dialog

+ GUI: Exit button and not close on exit (if not using embedded setting)
	Exit must shutdown before exit

+ Maybe dangerous senteces like PSTT that needs to be removed
	Take out timestamp
	Crop to VDM's (CropTransformer)

+ Maybe VDO converter needs to be able to convert BS -> AI
	(Talker transformer)

+ Anonymous: Random country + counter

+ Anonymous: Option to use random name file

+ Anonymous: Random callsign (UUID)

+ Server:Rest API with services to get list of targets
	Status of server, connected clients etc

+ Reduce stacktrace dumps

+ Investiage why server blocks on shutdown


+ Investigate if we are allowed to use a dump of a restricted area to public use

+ Time stamping in stream option (Maybe GPS message see EPD-ship)

+ Documentation

+ Setup on cloudbees


